# Copy to .env and fill in your hostname.
#
# DOMAIN is where both workers are reachable. The proxy takes /v1/*
# and /_cm/*; the dashboard takes everything else. CF Access (Google
# IdP) should gate the hostname with Bypass rules for /v1/* and /_cm/*.
DOMAIN=your-domain.example
